function new() {
    // default options on startup
    if (FlxG.save.data.camZooming == null) FlxG.save.data.camZooming = true;
    if (FlxG.save.data.notecam == null) FlxG.save.data.notecam = false;
    if (FlxG.save.data.iconBop == null) FlxG.save.data.iconBop = false;
}